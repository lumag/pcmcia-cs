# Parallel device configuration
#
# The address format is "scheme,socket,instance".
#
case "$ADDRESS" in
*,*,*)
    # Symbolic link?
    #LINK="/dev/printer"
    # Options for 'tunelp'
    #LP_OPTS=""
    ;;
esac
